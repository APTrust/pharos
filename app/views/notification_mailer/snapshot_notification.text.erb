Team,

Here are the latest snapshot results for the <%= Rails.env.capitalize %> repository broken down by institution.

  <% @snap_hash.each do |key, value| %>
      <%= key %> : <%= value %>
  <% end %>