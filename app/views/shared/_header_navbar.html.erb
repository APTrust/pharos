<div id="header-navbar" class="navbar navbar-default navbar-static-top" role="navigation">
  <div class="container">

    <div class="navbar-header col-md-3">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#user-util-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <%= link_to image_tag("aptrust/logoWhite.png", height: 60, width: 325, border: 0, alt: "APTrust Logo"), root_path, class: "navbar-brand logo-text" %>
    </div>

    <div class="search-container">
      <div id="search-navbar" class="navbar-static-top col-md-6" role="navigation">
        <div class="navbar-header">
          <% if user_signed_in? %>
              <%= render partial: '/shared/search_form' %>
          <% end %>
        </div>
        <br><br>
        <div id="navigation_tabs">
          <% if user_signed_in? %>
              <ul class="nav nav-tabs">
                <li><%= link_to 'Institutions', root_path %></li>
                <li><%= link_to 'Objects', intellectual_objects_path(@institution, sort: 'date') %></li>
                <li><%= link_to 'Files', institution_files_path(@institution, sort: 'date') %></li>
                <li><%= link_to 'Events', institution_events_path(@institution) %></li>
                <li><%= link_to 'Work Items', work_items_path %></li>
                <% if current_user.admin? %>
                    <li><%= link_to 'DPN Items', dpn_work_items_path %></li>
                <% end %>
                <li><%= link_to 'Reports', reports_path(@institution.identifier) %></li>
              </ul>
          <% end %>
        </div>
      </div>
    </div>

    <div class="collapse navbar-collapse col-md-3" id="user-util-collapse">
      <% if user_signed_in? %>
          <%= render partial: '/user_util_links' %>
      <% else %>
          <%= render partial: '/user_sign_in_links' %>
      <% end %>
    </div>

  </div>

</div>