<% if @response.total_pages > 1 %>
    <div class="row record-padding">
      <div class="col-md-9">
        <div class="pagination">
          <%= paginate @response, :outer_window => 2f %>
        </div>
      </div>
    </div>
<% end %>